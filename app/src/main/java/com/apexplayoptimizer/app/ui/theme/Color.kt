package com.apexplayoptimizer.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Surfaces ──────────────────────────────────────────────────────────────────
val Background   = Color(0xFF09090E)
val Surface      = Color(0xFF10121C)
val Card         = Color(0xFF141620)
val CardBorder   = Color(0xFF1E2132)
val DividerColor = Color(0xFF181A28)

// ── Accents ───────────────────────────────────────────────────────────────────
val Primary      = Color(0xFF4C8BF5)   // Electric blue  — main brand
val PrimaryDim   = Color(0x334C8BF5)

val Orange       = Color(0xFFF97316)   // Warm orange
val OrangeDim    = Color(0x33F97316)

val Blue         = Color(0xFF4C8BF5)   // alias for Primary (kept for compatibility)
val Purple       = Color(0xFF8B5CF6)   // Violet
val Yellow       = Color(0xFFF59E0B)   // Amber
val Teal         = Color(0xFF14B8A6)   // Teal

// ── Semantic ──────────────────────────────────────────────────────────────────
val Success      = Color(0xFF22C55E)   // Emerald green  (positive / done)
val Danger       = Color(0xFFEF4444)   // Red
val Warning      = Color(0xFFF59E0B)   // Amber (same as Yellow)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF1F5F9)
val TextSecondary = Color(0xFF8B9DC3)
val TextMuted     = Color(0xFF4A5568)

// ── Gauges ────────────────────────────────────────────────────────────────────
val GaugeTrack   = Color(0xFF1E2132)
val SliderTrack  = Color(0xFF252840)
